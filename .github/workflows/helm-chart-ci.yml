name: Helm chart CI

on:
  push:
    branches: [main]
    paths:
      - 'Chart.yaml'
      - 'values.yaml'
      - 'values/**'
      - 'README.md'
      - '.helmignore'
      - 'templates/**'
  pull_request:
    branches: [main]
    paths:
      - 'Chart.yaml'
      - 'values.yaml'
      - 'values/**'
      - 'README.md'
      - '.helmignore'
      - 'templates/**'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      baseline_version:
        description: 'First release version when no tags exist (e.g. 1.0.0)'
        required: false
      release_tag:
        description: 'Release tag override (e.g. immich-v1.0.0)'
        required: false

concurrency: helm-chart-immich

jobs:
  ci:
    permissions:
      contents: write
      pull-requests: read
    uses: expectedbehaviors/github-actions/.github/workflows/helm-chart-ci.yml@main
    secrets: inherit
    with:
      chart_path: .
      chart_name: immich
      tag_prefix: immich-v
      helm_repositories: |
        immich=https://immich-app.github.io/immich-charts
        onepassworditem=https://expectedbehaviors.github.io/OnePasswordItem-helm
      helm_repo_name: immich
      helm_repo_url: https://immich-app.github.io/immich-charts
      helm_repo_name_2: onepassworditem
      helm_repo_url_2: https://expectedbehaviors.github.io/OnePasswordItem-helm
      publish_enabled: true
      release_notes_enabled: true
      initial_version: ${{ github.event.inputs.baseline_version || '' }}
      release_tag: ${{ github.event.inputs.release_tag || '' }}
